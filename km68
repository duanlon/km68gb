<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Tìm kiếm ID Game - Demo</title>
  <style>
    :root{
      --bg-deep:#0e3b4a;
      --card-bg:#f3e0b9;
      --accent:#f0c419;
      --dark:#2b4150;
      --pill:#ffd54d;
      --shadow: 0 8px 20px rgba(12,30,36,0.15);
      --radius:12px;
      font-family: Inter, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
    }

    body{
      margin:0;
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      background: var(--bg-deep);
      color:var(--dark);
    }
    body::before{
      content: "";
      position:fixed; inset:0; z-index:-1;
      background-image:
        radial-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
        linear-gradient(180deg, rgba(0,0,0,0.06), rgba(255,255,255,0.02));
      background-size: 6px 6px, 100% 100%;
      opacity:0.6;
      mix-blend-mode: overlay;
    }

    .container{
      width:min(980px, 94vw);
      background: var(--card-bg);
      border-radius:var(--radius);
      box-shadow: var(--shadow);
      padding:28px;
    }

    h1{ margin:0 0 14px 0; color: #153d45; font-size:20px; font-weight:700; }

    .search-row{ display:flex; gap:12px; align-items:center; margin-bottom:12px; }
    .search-row input{ flex:1; padding:12px 14px; border-radius:10px; border:2px solid rgba(0,0,0,0.15); font-size:15px; outline:none; background:transparent; }
    .btn{ background:var(--accent); border:none; padding:10px 16px; border-radius:8px; font-weight:700; cursor:pointer; }

    .hint{ color: #3a5a60; font-size:13px; margin-bottom:14px; }
    .msg{ margin-top:10px; color:#7a2b2b; font-weight:700; }

    table{ width:100%; border-collapse:separate; border-spacing:0 12px; font-weight:600; }
    thead th{ background:var(--dark); color:white; text-align:left; padding:14px 18px; border-radius:10px; font-weight:800; }
    tbody td{ padding:18px; background: rgba(0,0,0,0.02); border-radius:8px; vertical-align:middle; }
    .status-pill{ display:inline-block; padding:8px 12px; background:var(--pill); border-radius:24px; font-weight:800; color:#27363a; }

    .center{ text-align:center; }
  </style>
</head>
<body>
  <div class="container" role="main">
    <h1>Tìm kiếm ID Game</h1>
    <div class="search-row">
      <input id="gameId" inputmode="numeric" pattern="[0-9]*" placeholder="Nhập ID game">
      <button id="searchBtn" class="btn">Tìm</button>
    </div>
    <div id="hint" class="hint">Vui Lòng Nhập Chính Xác ID Game</div>
    <div id="message" class="msg"></div>

    <div id="tableWrap" style="display:none;">
      <table>
        <thead>
          <tr>
            <th>NẠP</th>
            <th>KHUYẾN MÃI</th>
            <th>TRẠNG THÁI</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td class="center">5.000.000 đ</td>
            <td class="center">2.000.000 đ</td>
            <td class="center"><span class="status-pill">ĐỦ ĐIỀU KIỆN</span></td>
          </tr>
          <tr>
            <td class="center">10.000.000 đ</td>
            <td class="center">4.000.000 đ</td>
            <td class="center"><span class="status-pill">ĐỦ ĐIỀU KIỆN</span></td>
          </tr>
          <tr>
            <td class="center">15.000.000 đ</td>
            <td class="center">7.000.000 đ</td>
            <td class="center"><span class="status-pill">ĐỦ ĐIỀU KIỆN</span></td>
          </tr>
          <tr>
            <td class="center">20.000.000 đ</td>
            <td class="center">9.000.000 đ</td>
            <td class="center"><span class="status-pill">ĐỦ ĐIỀU KIỆN</span></td>
          </tr>
          <tr>
            <td class="center">30.000.000 đ</td>
            <td class="center">14.000.000 đ</td>
            <td class="center"><span class="status-pill">ĐỦ ĐIỀU KIỆN</span></td>
          </tr>
          <tr>
            <td class="center">50.000.000 đ</td>
            <td class="center">20.000.000 đ</td>
            <td class="center"><span class="status-pill">ĐỦ ĐIỀU KIỆN</span></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  <script>
    const input = document.getElementById('gameId');
    const btn = document.getElementById('searchBtn');
    const msg = document.getElementById('message');
    const tableWrap = document.getElementById('tableWrap');

    input.addEventListener('input', (e)=>{
      const cleaned = e.target.value.replace(/\D+/g, '');
      if(cleaned !== e.target.value) e.target.value = cleaned;
      msg.textContent = '';
    });

    function showError(text){
      msg.textContent = text;
      tableWrap.style.display = 'none';
    }
    function showTable(){
      msg.textContent = '';
      tableWrap.style.display = '';
    }

    btn.addEventListener('click', ()=>{
      const v = input.value.trim();
      if(!v){ showError('ID không tồn tại.'); return; }
      if(v.length < 8){ showError('ID không tồn tại.'); return; }
      if(!/^\d+$/.test(v)){ showError('ID không tồn tại.'); return; }
      const num = Number(v);
      if(num > 29999999){ showError('ID không tồn tại.'); return; }
      showTable();
    });

    input.addEventListener('keydown', (e)=>{ if(e.key === 'Enter') btn.click(); });
  </script>
</body>
</html>
